{
  "name": "powershell-conventions",
  "version": "0.1.0",
  "status": "active",
  "description": "Always-on PowerShell correctness rules: wrap any call to a function returning a variable-count collection in @() at every call site, and never embed a literal backslash inside a joined path string meant to run cross-platform.",
  "origin": {
    "note": "Distilled from two 2026-07 incident notes captured during ai-toolkit/es-ai-toolkit baseline.ps1 status-command work: an array-unwrap bug masked by pwsh 7's synthetic scalar .Count convenience (only surfaced when tested under real Windows PowerShell 5.1), and a literal-backslash-in-path bug that silently breaks Join-Path calls under macOS/Linux pwsh."
  },
  "adapters": {
    "codex": "adapters/AGENTS.md.block",
    "claude": "adapters/CLAUDE.md.block",
    "copilot": "adapters/copilot-instructions.md.block"
  }
}
